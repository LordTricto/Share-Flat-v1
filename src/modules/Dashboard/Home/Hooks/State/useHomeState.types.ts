export interface UseHomeStateInterface {
	// main: MainInitState | null;
	// inflow: Array<Transaction>;
	// outflow: Array<Transaction>;
	// pending: Array<Transaction>;
	// isLoaded: boolean;
	// showAmount: boolean;
	// dashboardDetailsResponse: DashboardDetailsResponse | null;
	// accountsData: {
	// user: User;
	// userAccountsMeta: Array<UserAccountMeta>;
	// company: Corporate;
	// accounts: Array<UserAccount>;
	// userGroups: Array<UserGroup>;
	// transactionCategories: Array<TransactionCategory>;
	// } | null;

	handleToggleShowAmount: () => void;
}
