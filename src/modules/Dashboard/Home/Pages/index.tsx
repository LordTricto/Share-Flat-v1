// import InflowCard, {TransactionCardType} from "../Components/Cards/InflowCard";
import React from "react";
function DashboardHome(): JSX.Element {
	// const {main, dashboardDetailsResponse, accountsData, inflow, outflow, pending, isLoaded, showAmount, handleToggleShowAmount} = useHomeState();
	// window.history.replaceState({}, "Home", "/");

	return <>test</>;
}

export default DashboardHome;
